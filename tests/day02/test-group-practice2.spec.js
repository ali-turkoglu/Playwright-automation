import { test } from "@playwright/test";

test.describe("Test Group", () => {
  test("A", async ({ page }) => {
    // test body
  });

  test("B", async ({ page }) => {
    // test body
  });

  test("C", async ({ page }) => {
    // test body
  });
});
